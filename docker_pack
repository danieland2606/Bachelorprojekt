#! /bin/sh
docker compose --profile rda --profile eda build

docker images meowmed*.eda --format '{{.Repository}}' | tr '\n' ' ' | xargs docker save -o meowmed-eda.tar
docker images meowmed*.rda --format '{{.Repository}}' | tr '\n' ' ' | xargs docker save -o meowmed-rda.tar